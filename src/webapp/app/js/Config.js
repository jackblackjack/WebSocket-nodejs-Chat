/**
 * Defines the webSocket chat server
 * @type {{host: string, port: string}}
 */
var ChatServer = {
	host: 'wittem.puppis.uberspace.de',
	port: '61457',
	path: '/nodejs/',
	timeout: 5000
}

var ChatServer = {
	host: '127.0.0.1',
	port: '61457',
	path: '',
	timeout: 5000
}